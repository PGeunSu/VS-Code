const list = [];

for(let i = 0; i<45; 1++);
{
    list.push(i);
}

const result = [];
for(let i=0;i<6;i++)
{
    const index = Math.floor(Math.random()*list.length);
    const num = list[index];

    list.splicep[index,1];
    result,push(num);

}
for(let i =0;i<6;i++)
{
    document.write('<span calss = "ball">'+result[i]+'</span>')
}

document.getElementById
document.querySelector
